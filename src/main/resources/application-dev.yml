spring:
  datasource:
    url: jdbc:mysql://localhost:3306/community
    username: root
    password:

  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        generate_statistics: true
